
{{#each blogs as |blog| }}
<div class="row mb-4 blog">
  <div class="col-md-5">
    <h2>{{blog.title}}</h2>
    <p>
      {{blog.body}}
    </p>
    <p>Created by {{blog.user.username}}</p>
  </div>
  <div class="col-md-7">
  </div>
</div>
{{/each}}
